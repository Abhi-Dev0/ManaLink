server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: dev

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Abhi-Dev0/ManaLink.git
          default-label: init-setup
          timeout: 4
          search-paths: config/{profile}
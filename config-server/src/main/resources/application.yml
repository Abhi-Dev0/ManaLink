server:
  port: 8080

spring:
  application:
    name: config-server

  profiles:
    active: dev

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Abhi-Dev0/ManaLink.git
          timeout: 4
          search-paths: config/{profile}